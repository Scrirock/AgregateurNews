{% extends 'base.html.twig' %}

{% block title %}{{ data.title }}{% endblock %}

{% block body %}

    <a href="/" id="home">Retour a l'accueil</a>

    <h1>{{ data.title }}</h1>

    <p class="descriptionUnique">{{ data.description }}</p>
    <a href="{{ data.url }}" class="uniqueLink">Lire l'article sur un autre site</a>

{% endblock %}
